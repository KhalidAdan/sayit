{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main (hidden) window and waveform overlay",
  "windows": ["main", "waveform"],
  "permissions": [
    "core:default"
  ]
}
