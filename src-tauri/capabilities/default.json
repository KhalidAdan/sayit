{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main (hidden) window, waveform overlay, and dictionary editor",
  "windows": ["main", "waveform", "dictionary"],
  "permissions": [
    "core:default"
  ]
}
