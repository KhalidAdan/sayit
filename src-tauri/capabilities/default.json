{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main (hidden) window",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
